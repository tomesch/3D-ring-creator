define(['three'], function (THREE) {
  'use strict';
  return {
    sections: [
      
    ],
    getGeometry: function () {
      var geometry = new THREE.Geometry();
      return geometry;
    }
  };
});
